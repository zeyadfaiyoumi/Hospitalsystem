// import { createSlice } from '@reduxjs/toolkit';

// export const doctorActions = {
//   setLoading: () => ({ type: 'doctors/setLoading' }),
//   setDoctors: (doctors) => ({ type: 'doctors/setDoctors', payload: doctors }),
//   setError: (error) => ({ type: 'doctors/setError', payload: error }),
// };
